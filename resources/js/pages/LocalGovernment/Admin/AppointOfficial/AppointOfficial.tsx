import AppLayout from '@/layouts/App/AppLayout';

export default function AppointOfficials({ term, positions, municipality }: any) {
    console.log(term);
    console.log(positions);
    console.log(municipality);
    return (
        <AppLayout>
            <div></div>
        </AppLayout>
    );
}
