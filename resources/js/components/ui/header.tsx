import React from 'react';

const Header= () => (
    <header className="fixed top-0 left-0 w-full bg-white shadow-sm z-50 p-4">
        <h1 className="m-0 text-xl font-bold">My App</h1>
    </header>
);

export default Header;
