export type EventFormData = {
    title: string;
    description: string;
    event_date: string;
    id: string;
};

export type EventData = {
    title: string;
    description: string;
    event_date: string;
    id: string;
};
